export async function getAdvPayments() {
  const rawResponse = await fetch('/api/advPayments', {
    method: 'GET',
    credentials: 'same-origin',
    headers: {
      'Accept': 'application/json',
      'Content-Type': 'application/json',
    },
  })
  return rawResponse.json()
}
